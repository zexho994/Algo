package leetcode.medium;

import struct.Node;


public class Connect {
    public Node connect(Node root) {
        if (root == null) {
            return null;
        }
        int level = 0;
        Node low = root;
        Node next = null;
        Node cur = root;
        while (low != null) {
            

        }
        return root;
    }

}
